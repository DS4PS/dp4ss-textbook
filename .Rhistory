knitr::opts_chunk$set( echo = TRUE, message=F, warning=F, fig.width=10 )
library( geojsonio )
library( sp )
library( rgdal )
syr <- geojson_read( "syr_parcels.geojson", method="local", what="sp" )
plot( syr,  border=NA, col="gray80" )
syr <- geojson_read( "syr_parcels.geojson", method="local", what="sp" )
library( geojsonio )
library( sp )
library( rgdal )
URL <- "https://raw.githubusercontent.com/DS4PS/Data-Science-Class/master/DATA/syr_parcels.geojson"
syr <- geojson_read( URL, method="local", what="sp" )
plot( syr,  border=NA, col="gray80" )
nrow( syr@dat )
nrow( syr@data )
these <- dat$acres > 1
group.colors <- ifelse( these, "firebrick", "gray80" )
plot( syr,  border=NA, col=group.colors )
URL <- "https://raw.githubusercontent.com/DS4PS/Data-Science-Class/master/DATA/syr_parcels.csv"
dat <- read.csv( URL, stringsAsFactors=FALSE )
these <- dat$acres > 1
group.colors <- ifelse( these, "firebrick", "gray80" )
plot( syr,  border=NA, col=group.colors )
table( dat$land_use )
these <- dat$land_use == "Single Family"
group.colors <- ifelse( these, "firebrick", "gray80" )
plot( syr,  border=NA, col=group.colors )
unique( dat$land_use )
knitr::opts_chunk$set(echo = TRUE, message=F, warning=F)
library( dplyr )
library( pander )
these <- dat$land_use == "Single Family"
mean( these )
these <- dat$land_use == "Single Family"
group.colors <- ifelse( these, "firebrick", "gray80" )    # don't change this
plot( syr,  border=NA, col=group.colors )                 # don't change this
class( dat$land_use )
sum( dat$land_use == "Apartment" )
sum( dat$land_use == "apartment" )
these  <-  dat$land_use == "Community Services" |
dat$land_use == "Schools" |
dat$land_use == "Recreation"
group.colors <- ifelse( these, "firebrick", "gray80" )    # don't change this
plot( syr,  border=NA, col=group.colors )                 # don't change this
sum( dat$assessedva )
sum( dat$amtdelinqu > 0 )
mean( dat$amtdelinqu > 0 )
table( dat$amtdelinqu > 0, dat$land_use )
